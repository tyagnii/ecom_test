## Development overrides for docker-compose
## This file is automatically loaded by docker-compose
#services:
#  # Override API service for development
#  api:
#    environment:
#      # Enable debug logging
#      LOG_LEVEL: debug
#    volumes:
#      # Mount source code for hot reloading (if using air or similar)
#      - ../:/app/src:ro
#    command: ["./main", "api", "--port", "8080"]
#
#  # Override PostgreSQL for development
#  postgres:
#    environment:
#      # Enable more verbose logging
#      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=C"
#    volumes:
#      # Mount init scripts
#      - ./init-scripts:/docker-entrypoint-initdb.d:ro
#      # Persist data
#      - postgres_dev_data:/var/lib/postgresql/data
#
#  # Development database seeding
#  seed:
#    build:
#      context: ..
#      dockerfile: Dockerfile
#    container_name: ecom_seed
#    environment:
#      DB_HOST: postgres
#      DB_PORT: 5432
#      DB_USER: postgres
#      DB_PASSWORD: postgres
#      DB_NAME: ecom_test
#      DB_SSLMODE: disable
#    command: ["./main", "banner", "create", "Development Banner"]
#    depends_on:
#      migrate:
#        condition: service_completed_successfully
#    networks:
#      - ecom_network
#    restart: "no"
#
#volumes:
#  postgres_dev_data:
#    driver: local
